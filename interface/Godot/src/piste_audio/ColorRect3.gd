extends ColorRect


var mouse_in := false

var stream:AudioStream

func update_visual():
	stream = get_parent().stream
	
	stream.
# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass


